import {styled, IconButton} from "@mui/material";

export const StyledIconButton = styled(IconButton)`
  && {
    position: absolute;
    left: 10px;
    top: 10px;

    .MuiSvgIcon-root {
      font-size: 25px;
    }
  }

`